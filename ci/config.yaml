# yaml-language-server: $schema=https://raw.githubusercontent.com/camptocamp/c2cciutils/1.2.16/c2cciutils/schema.json

publish:
  pypi:
    versions:
      - version_tag
      - version_branch
      - rebuild

audit:
  snyk:
    files_no_install:
      - example-project/ci/requirements.txt
checks:
  required_workflows: false
  codespell: false
