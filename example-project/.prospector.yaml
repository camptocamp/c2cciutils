inherits:
  - utils:base
  - utils:no-design-checks
  - duplicated

mypy:
  options:
    python-version: '3.10'
    ignore-missing-imports: True
    warn:
      - redundant-casts
      - unused-ignores
    check:
      - untyped-defs

bandit:
  options:
    config: .bandit.yaml
